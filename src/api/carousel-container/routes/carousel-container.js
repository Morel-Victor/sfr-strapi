'use strict';

/**
 * carousel-container router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::carousel-container.carousel-container');
