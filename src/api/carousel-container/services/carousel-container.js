'use strict';

/**
 * carousel-container service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::carousel-container.carousel-container');
